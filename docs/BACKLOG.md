# Fika ブラッシュアップ・バックログ

2026-06-11 の調査ワークフロー（競合監査・AI連携・休憩UX・統計UX の4軸 + 現実装のデザイン批評、エージェント6体）の統合結果。

> **2026-08-20 更新:** Issue #57 で履歴・集計・セッション数・メモ・検索・JSONL・SQLite/GRDBを廃止した。以下の過去項目より `REQUIREMENTS.md` の「今回だけを扱う4モード」方針を優先し、記録機能は追加候補へ戻さない。

## 反映済み（2026-06-11）

- ✅ justFinished グローが休憩中ずっと残るバグ → 6秒で自動消灯
- ✅ グローのハロー光が clipShape で削られる → クリップ外の overlay + 12px 余白
- ✅ idle 時にボタンが見えない初回体験 → 待機中はコントロール常時表示、「開始待ち」→「いつでもどうぞ」
- ✅ 貯めた休憩ピルをボタン化（報酬をクリックして受け取る構造）＋全ボタンにツールチップ
- ✅ メニュー刷新: 「透明度」→「集中時の濃さ」（逆読み防止）、モード別出し分け、説明的コピー
- ✅ 表示が嘘をつく3点: ポモドーロ待機中は次の作業時間を予告表示 / フロー進捗バーは25分超で減光 / 1時間到達時のフォントジャンプ廃止（自動縮小）
- ✅ 休憩スキップに3秒クールダウン（one sec の PNAS 研究: 「自分で閉じる選択肢＋摩擦」が最も効果が持続。+5分と小さくは即時のまま）
- ✅ pomo CLI＋ローカルAPIを試作（App Sandboxと衝突するため2026-06-13に削除済み）
- ✅ タイマーモード（任意時間のカウントダウン）— 休憩や作業記録に接続せず、パネル待機時の [−5分][▶][+5分] で操作する

## 反映済み（2026-06-13・App Store 提出準備パス）

監査ワークフロー（App Store 適合・レスポンシブ・デザイン/a11y・機能/オンボーディングの4軸）→ 実装。

- ✅ **localhost API を全削除**（App Sandbox と衝突。`APIServer.swift`・`scripts/pomo`・CLAUDE.md API 節を撤去）
- ✅ **Dock 非表示の回帰修正**（`setActivationPolicy(.accessory)` + Info.plist `LSUIElement`。母艦ウィンドウ追加時に accessory 化が抜けていた）
- ✅ **システム通知 M5 第2合図**（`NotificationManager`。初回完了直前に許可要求、作業/休憩おわりにアクション付き通知＝別 Space でも気づける）
- ✅ **初回オンボーディング**（初回起動で母艦ウィンドウを開く。`didOnboard`）
- ✅ **オーバーレイ事前警告**（残60秒で `isApproachingEnd` → パネル進捗バーの色温度を琥珀→濃琥珀へ。突然落とさない）
- ✅ **会議ガードの休憩中再チェック**（通話で延期したオーバーレイを、通話終了後10秒以内に自動表示）
- ✅ **レスポンシブ**（設定の固定幅コントロール撤去・母艦のmax幅センタリング）
- ✅ **アクセシビリティ**（Dynamic Type 対応 `pomoFont`・WCAG AA コントラスト `sumiSecondary/kohakuText`・VoiceOver ラベル・Reduce Motion 尊重）
- ✅ **App Store 構成**（`Pomo.entitlements` app-sandbox・MAS 必須 Info.plist キー・XcodeGen `project.yml`・アイコン asset catalog）
- ✅ サウンド制御を実装。統計は Issue #57 で廃止

## 次にやる（大きめの賭け）

| 項目 | 根拠 | 工数 |
|------|------|------|
| README に信条を明文化（アカウントなし・テレメトリなし・サブスクなし・罪悪感なし） | TomatoBar は信条の明文化を信頼の感情価値に変えて 2,800★。「time blindness」という痛みの名指しが機能列挙より刺さる | S |
| macOS 集中モード（DND）連動（作業開始で ON、Shortcuts 経由なら private API 不要） | ブロッカーを作らずに集中を守れる唯一のミニマル整合な手段 | M |
| 配布の実務（Apple Developer Program・署名・公証・スクショ・LP） | `docs/APP_STORE_SUBMISSION.md` 参照。ご主人様のアカウント作業が要る外部ゲート | M |

## 2026-08-20 の整理

- 母艦ウィンドウは「タイマー / 設定 / 願い」の3ページで実装済み。
- 記録・検索・DBの調査案は撤回した。今回の時間だけをメモリで扱い、設定以外は保存しない。
- 追加案を検討するときも、過去の比較や整理を促す機能は `REQUIREMENTS.md` の「やらないこと」を先に確認する。

## やらない（調査で根拠が確定）

| 項目 | 理由 |
|------|------|
| アカウント・クラウド同期・サブスク課金 | HN で最頻出の称賛が「アカウント不要」。Session は「機能は素晴らしいが価格が silly」と切り捨てられた。ローカルオンリー無料は欠点ではなく堀 |
| スキップ不能の強制ロック休憩（デフォルト） | Opal の事例: ユーザーは遵守ではなく回避策探しに走る。PNAS 研究でも最強要素は「自分で閉じる選択肢」 |
| ストリーク・ヒートマップ・ゲーミフィケーション | Forest の罪悪感メカニクスは「飽きる・長期効果弱い」が定着。ヒートマップの空白セルはストリークの変装。no-guilt 方針と正面衝突 |
| AI コーチ・割り込み検知・生産性スコア・ムード入力 | Centered（アプリ切替検知＋音声ナッジ）は「推測しすぎ・侮辱的」と酷評され倒産。判定・評価はストリークフリー思想と相反 |
| 単体 stdio MCP サーバー / URL スキーム / localhost API 全般 | localhost API は App Sandbox（MAS 必須）と衝突するため 2026-06-13 に廃止。復活させない。MCP もコンテキストを大量消費し、エージェント連携より Sandbox 適合・配布を優先する判断 |
